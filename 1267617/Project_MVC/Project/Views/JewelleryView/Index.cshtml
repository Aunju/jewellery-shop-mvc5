@model IEnumerable<Project.Models.Items>
@{
    ViewBag.Title = "Index";
}

@*<div class="row">
        <div class="col" style="width:18rem;">
            @foreach (var t in Model)
            {

                <div class="card">
                    <link href="~/Content/StyleSheet.css" rel="stylesheet" />
                    <img src="@Url.Content(t.PicturePath)" width="100" height="200" style="margin-bottom:10px;" class="card-img-top">
                    <h4><b>  @Html.DisplayFor(x => t.ItemName)</b></h4>
                    <h5> @Html.DisplayFor(x => t.Category.CategoryName)</h5>
                    <p>Price:@Html.DisplayFor(x => t.Price)</p>
                    <p class="btn btn-success"><button>Add to Cart</button></p>
                </div>


            }
        </div>
    </div>*@


<div class="row">
        <h2 class="text-center">Jewellery List</h2><br/>
        @foreach (var item in Model)
        {
<div class="col-md-3" style="margin-bottom:30px">
    <link href="~/Content/StyleSheet.css" rel="stylesheet" />
    <div style="text-align:center ; border:2px solid maroon">
        <h4>@item.ItemName</h4>
    </div>
    <div class="img-thumbnail">
        <div class="image" style="margin:20px">

            <img src="@Url.Content(item.PicturePath)" width="150" height="200" class="img-responsive">
        </div>
        <div class="col-md-4" style="text-align:left">
            <h4>@item.Category.CategoryName</h4>
        </div>
    </div>
    @*<div class="col-md-8" style="font-size: 10px; border-top: 2px solid maroon; padding:2px">
        <h5>@item.Price.ToString(format: "0.00")</h5>
    </div>*@
    <div class="col-md-8" style="text-align: center; margin-bottom: 20px; border-top: 2px solid maroon; padding: 2px ">
        @*<input type="button" name="AddToCart" value="Add To Cart" class="btn btn-primary" pId="@item.Id" id="btnAddToCart" onclick="AddToCart(this.AddToCart)" />*@
        @Html.ActionLink("Details...", "Details", new { id = item.Id }, new { @class = "btn btn-success" })
    </div>


</div>
    }
</div>
